rootProject.name = 'idv'

include 'core:identity-core'
include 'core:verification-context-core'

include 'dao:identity:identity-dao-fake'
include 'dao:identity:identity-dao-dynamodb'

include 'plugins:identity:alias-loader'

include 'json-api'
include 'json-api:identity-json-api'

include 'aws-lambda'
include 'aws-lambda:identity-aws-lambda'